# mekong-dem-90m.bin is a BUILD OUTPUT: sim/tools/bake_dem.py regenerates it deterministically from
# the `mekong` row of its region table against a local fb-tiles (doc/assets.md §0, "version the recipe,
# never the cake"). 38 MB per round buys nothing a seven-minute bake does not restore.
*.bin
