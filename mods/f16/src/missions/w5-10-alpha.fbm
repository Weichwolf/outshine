name w5-10-alpha
timeout 700
time 2018-01-15T20:00:00Z
wx fixture wx-2026-07-27T00Z.wxb

# CAMPAIGN W5, SORTIE 10 OF 10 - BALTIC AIR POLICING / QRA: IDENTIFICATION AS THE TASK.
# THE FULL ALPHA SCRAMBLE - NIGHT, CLOUD, FOUR JETS, THREE CONTACTS, WEAPONS HOLD.
# Spec, anchor and sources: doc/campaigns/w5-baltic-qra.md. Format: doc/missions/campaign.md.
# CONTROL: none - it is the capstone. Its parts are controlled in 04, 07, 08 and 09.
#
# THE ARENA, and every coordinate in it is [SET]. NATO Baltic Air Policing flies out of Siauliai
# (Zokniai) AB, Lithuania, ~55.89 N 23.39 E [T4]; the intercept happens over the Baltic west of the
# Lithuanian coast, on a track running east toward the airspace boundary. Flown under `--elev const`
# on a 0 m datum and with NO TERRAIN MASKING (gap C4).
#   the coastal surveillance radar  a P-18, `set emcon free`, 55.80 N 21.05 E. GAP C1 IS CLOSED AND
#                                   THIS CAMPAIGN RE-CHECKED IT: W5's own spec still says "coastal
#                                   radars do not emit ... what the QRA protects is scenery". It is not
#                                   scenery any more - it radiates, it is heard on the intruder's
#                                   receiver, it builds its own tracks and it can be killed
#   the port                        Klaipeda, ~55.70 N 21.13 E [T4], a `target_hard`
#   the second coastal site         Palanga, ~55.92 N 21.07 E [T4], a `target_soft`
# NOTHING ON THE GROUND IS A TARGET IN THIS CAMPAIGN. They are what the intercept protects, and they
# are declared so the protection has objects. `objective protect` is deliberately NOT used: it names a
# UNIT, and a policing sortie protects an AREA.
# Scale at this latitude: 1 deg of latitude = 111 132 m, 1 deg of longitude = 62 411 m [DERIVED].
# THERE ARE NO SHIPS (gap C14) and the Baltic is full of them: the one cast item this campaign asks
# for and cannot have.
#
# THE SUBJECT IS A TRANSPORT, AND THAT IS THE WHOLE DIFFERENCE. W5's spec lists this campaign as
# blocked on gap C7 - "no transport, ELINT or bomber module ... the subject of a real intercept is not
# a fighter, and substituting one changes the geometry, the closure and the whole point". RULE 7 SAYS
# RE-CHECK A BLOCKED MISSION AGAINST THE TREE AND NOT AGAINST THE GAP'S STATUS LINE, and the re-check
# says the capability is here: `an26` is a catalogue row (doc/modules/air/catalogue.md), it is a MOVER,
# and a mover has no generated deck - so the `ALPHA` verdict that keeps ten catalogue rows out of a
# campaign is a statement about GENERATED DECKS and there is none in this file. What the row has to do
# here is fly a straight line at a published speed, carry a transponder state and present a span to an
# eye; all three are sourced [T4]. It is also the anchor's Il-20 ELINT aircraft, because an ELINT
# aircraft RECEIVES and FlightBox has no ESM emission to model.
# ITS SPAN IS 29.3 m AND THAT IS THE NUMBER THIS CAMPAIGN TURNS ON: the eye resolves on presented
# extent, so the transport is IDENTIFIED where a fighter is not even recognised.
#
# WHAT IS DECLARED AND WHAT IS NOT. `objective identify` measures the GEOMETRY - a planar range held
# for a cumulative dwell - and never a sensor event, because producing "this unit discriminated that
# unit" would mean building a contact->unit correlation, and that function's EXISTENCE is the identity
# leak the tree is shaped to prevent (doc/missions/verdict.md). The sensor half is read BESIDE the
# verdict, out of `radar IFF_REPLY` and `vis RECOGNISED`/`vis IDENTIFIED`. THE PRICE IS KNOWN AND
# ACCEPTED: a pilot that flies the box with its eyes shut still scores - and sortie 09 turns that
# sentence into a measurement instead of a caveat. The box (2 000 m) and the dwell are [SET] per
# mission; there is no default, and W5's Knowledge 2 marks the abeam geometry [SET] because no source
# in the set publishes one. Aspect is deliberately not in the objective.
#
# THE STRUCTURAL DISCLOSURE, repeated in all ten files because a reader who skips it reads every
# result backwards:
#   1. THE PASS IS FLOWN BY ROUTE, NOT BY THE PILOT. `FBPilot` has no intercept-and-shadow behaviour,
#      and `set task intercept` is a SHOOTING task whose own machine calls this exact geometry an
#      ABORT: FBPilot.cpp:1040, "inside InterceptAbortRangeNm = 5.0 nm and never shot - that is no
#      longer an intercept". MEASURED in attribution run A1 (w5-baltic-qra.md, State): the identical
#      pass with `set task intercept` goes `eng_state=abort` at t = 5.1 s at 4.52 nm, turns away, and
#      identifies nothing in 300 s. So every pass in all ten files is a declared ROUTE, and what the
#      campaign measures is a geometry an author flew, never one a pilot chose.
#   2. THERE IS NO SCRAMBLE (gap C6). A `.fbm` spawns aircraft, it does not alert them, so the
#      sub-15-minute reaction that IS the real mission is in none of these files. Where a sortie needs
#      two flights at different readiness it expresses the delay as DISTANCE (sortie 10).
#   3. THERE IS NO RULES-OF-ENGAGEMENT STATE (gap C19). Nothing in the tree says "weapons hold".
#      `objective no_fire` JUDGES a hold, it cannot IMPOSE one, and a pilot on `route` never consults
#      its own warning receiver at all. The discipline this campaign is about is therefore OBSERVED and
#      never COMMANDED - sortie 07 is where that is measured rather than asserted.
#   4. `FBMissionRunner` ends the whole run at the first flight-monitor K.O. (`FirstFlightKo`).
#
# EVERYTHING AT ONCE, AND NOTHING NEW: night (09), cloud (08), an escorted bomber (04) and escorts
# that radiate (07). Nothing in this file is a mechanism the campaign has not already measured alone,
# which is what makes its result readable.
# FOUR INTERCEPTORS IN TWO FLIGHTS, AND THE STAGGER IS A DISTANCE. The anchor scrambles a second pair
# behind the first; a `.fbm` has no delayed spawn (gap C6), so the second flight starts 24 km further
# back on the same track, which is 93 s of its own transit. That is a stand-in for a readiness state
# and it is named as one - and it costs the second flight 400 s to reach the subject, because a stagger
# in TIME becomes a stagger in CLOSURE when the thing you are chasing is 117 kt slower than you.
# THE ROE EXCEPTION IS DECLARED HERE AND NOWHERE ELSE. W5's spec says a weapon event in this sortie is
# a FAIL "unless the subject shoots first". `no_fire` HAS NO EXCEPTION and deliberately so
# (doc/missions/verdict.md: "fired upon" is an RWR WARNING, not a fact, and a verdict that depends on
# what a unit heard is a verdict that can be talked out of). So the exception lives HERE, as a reading
# rule: if any Red unit releases first, a Blue release is not counted against this campaign, and the
# `no_fire` FAIL is reported with that context. NO RED UNIT IN THIS FILE CARRIES A WEAPON, so the
# exception is declared and NOT exercised - which is stated rather than hidden.
#
# THE ONE TACTICAL QUESTION: does a four-ship take a night intercept of three contacts, two of which
# are painting it, and bring every weapon home?
#
# READING RULE (binding): exit 0 with all four `no_fire` objectives met and the three `identify`
# objectives met is the pass. A single `sms RELEASE` or `gun BURST` is a FAIL and, per the exception
# above, is reported with the question of who shot first. Read also: how many of the three contacts the
# eye contributed to (expected: none, it is night), and whether the second flight adds anything at all
# beyond a second set of eyes 24 km late.
#
# MEASURED (fingerprint 0fca0dd4f36031f3, exit 3): THE WEAPONS HOLD HELD ON ALL FOUR JETS. THE
# IDENTIFICATION DID NOT, AND WHAT BROKE IT WAS NAVIGATION AND NOT SENSORS.
#   THE PASS. `mission IDENTIFIED` three times: the bomber by the lead at t=116.4 (1 097.2 m), an
#   escort by the lead at t=145.8 (780.5 m), and the bomber AGAIN by the second flight at t=443.9
#   (677.7 m), 327.5 s after the first. The third contact - the NORTHERN escort - was inside the
#   2 000 m box for a CUMULATIVE 6.3 s against a required 20, closest approach 1 838 m at t=170.1.
#   IT ENTERED THE BOX AND WAS NOT HELD, which is the difference between `identify` and `approach`.
#   WHY. This is sortie 04's formation exactly - 1 667 m abeam - flown in the fixture's wind. The
#   escorts are MiG-29s at 383 kt and the interceptors F-16s at 500 kt, and the guidance points the
#   nose at the waypoint without crabbing, so a slower aeroplane bows further downwind on the same leg.
#   MEASURED BEFORE THE FIX: the second flight's single 176 km leg bowed 3.4 km off track and its
#   closest approach to the bomber was 4 038 m - it never entered the box at all. Broken into 0.55 deg
#   vectors (34 km legs, which is what a controller's steers are) the same aeroplane identifies at
#   677.7 m. THE VECTOR IS NOT A WORKAROUND, IT IS THE MISSING HALF OF A ROUTE IN WIND, and the
#   3.4 km / 677.7 m pair is the number that says so.
#   THE NIGHT: `grep -c "vis " events.log` = 0. Four interceptors, three contacts, a 700 s run, and the
#   eye contributed nothing at all - sortie 09's zero at four times the scale.
#   THE DISCIPLINE: `no_fire` MET on all four jets. Eight AIM-120 and AIM-9 on the rails, master arm
#   ARMED, two N019s in `illum` painting the formation for the whole run, and the campaign-level
#   `campaign EXPENDED` line is ABSENT because nothing was expended in any of the ten sorties.
#   THE EXCEPTION DECLARED ABOVE WAS NOT EXERCISED: no Red unit in this file carries a weapon.

unit qalead
  team friendly
  module f16
  flight qra 1
  spawn 55.90450 18.45419 6200 90.0 500   # first flight: 9.1 km in trail of the bomber
  set gear up
  set fuel_pct 70
  set datalink on
  set fcr_mode acm_hud                       # it looks, and the LOOK is not the verdict
  set fcr_range_nm 40
  set iff_interrogator on                    # the challenging half of the APX-113
  set store 3 aim120
  set store 7 aim9
  set brief_master_arm arm
  wp 55.90450 19.00419 6200 500
  wp 55.90450 19.55419 6200 500
  wp 55.90450 20.10419 6200 500
  wp 55.90450 20.65419 6200 500
  wp 55.90450 20.90000 6200 500
  objective identify unit qasub range 2000 hold 20
  objective identify unit qaesc1 range 2000 hold 20
  objective identify unit qaesc2 range 2000 hold 20
  objective no_fire

unit qatwo
  team friendly
  module f16
  flight qra 2
  spawn 55.88784 18.45419 6350 90.0 500
  set gear up
  set fuel_pct 70
  set datalink on
  set fcr_mode acm_hud                       # it looks, and the LOOK is not the verdict
  set fcr_range_nm 40
  set iff_interrogator on                    # the challenging half of the APX-113
  set task formation
  set store 3 aim120
  set store 7 aim9
  set brief_master_arm arm
  objective no_fire

unit qathree
  team friendly
  module f16
  flight qra2 1
  spawn 55.90450 18.06960 6500 90.0 500   # second flight: 24 km further back = 93 s of its own transit
  set gear up
  set fuel_pct 70
  set datalink on
  set fcr_mode acm_hud                       # it looks, and the LOOK is not the verdict
  set fcr_range_nm 40
  set iff_interrogator on                    # the challenging half of the APX-113
  set store 3 aim120
  set store 7 aim9
  set brief_master_arm arm
  wp 55.90450 18.61960 6500 500
  wp 55.90450 19.16960 6500 500
  wp 55.90450 19.71960 6500 500
  wp 55.90450 20.26960 6500 500
  wp 55.90450 20.81960 6500 500
  wp 55.90450 20.90000 6500 500
  objective identify unit qasub range 2000 hold 20
  objective no_fire

unit qafour
  team friendly
  module f16
  flight qra2 2
  spawn 55.88784 18.06960 6650 90.0 500
  set gear up
  set fuel_pct 70
  set datalink on
  set fcr_mode acm_hud                       # it looks, and the LOOK is not the verdict
  set fcr_range_nm 40
  set iff_interrogator on                    # the challenging half of the APX-113
  set task formation
  set store 3 aim120
  set store 7 aim9
  set brief_master_arm arm
  objective no_fire

unit qasub
  team neutral
  module tu95
  spawn 55.90000 18.60000 6500 90.0 383
  set iff_xpdr off
  wp 55.90000 20.60000 6500 383

unit qaesc1
  team neutral
  module mig29
  spawn 55.91950 18.60000 7000 90.0 383      # sortie 04's formation exactly: 1 667 m abeam
  set gear up
  set fuel_pct 60
  set n019_mode rad
  set n019_emission illum                    # both escorts paint the QRA jets
  set rwr on
  set iff_xpdr off
  wp 55.91950 19.15000 7000 383
  wp 55.91950 19.70000 7000 383
  wp 55.91950 20.25000 7000 383
  wp 55.91950 20.60000 7000 383

unit qaesc2
  team neutral
  module mig29
  spawn 55.88950 18.60000 7000 90.0 383      # sortie 04's formation exactly: 1 667 m abeam
  set gear up
  set fuel_pct 60
  set n019_mode rad
  set n019_emission illum                    # both escorts paint the QRA jets
  set rwr on
  set iff_xpdr off
  wp 55.88950 19.15000 7000 383
  wp 55.88950 19.70000 7000 383
  wp 55.88950 20.25000 7000 383
  wp 55.88950 20.60000 7000 383

unit qarad
  module p18
  team friendly
  spawn 55.80000 21.05000 ground 0.0 0
  set emcon free

unit qaport
  module target_hard
  team friendly
  spawn 55.70000 21.13000 ground 0.0 0

unit qasite
  module target_soft
  team friendly
  spawn 55.92000 21.07000 ground 0.0 0

unit qasite2
  module target_soft
  team friendly
  spawn 56.05000 21.02000 ground 0.0 0

unit qasite3
  module target_soft
  team friendly
  spawn 55.60000 21.05000 ground 0.0 0
