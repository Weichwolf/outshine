Type: task
Parent: 1623
Area: sim

# Lay refuses an assignment the scene does not hold

The head guard in Journey::Lay returns false on EVERY failed assembly claim -- a mind whose
Assigned pair does not target the cast's assignment refuses the lay instead of logging and
driving anyway. Proven by a unit twin that builds the mislinked scene and watches Lay say no.


---

Closed: the guard is one predicate (`assigned`) claimed and enforced at src/sim/Journey.cpp;
proving test test/unit/sim/ALayRefusesASceneItCannotDrive -- the mislinked mind refuses the lay
with zero transport asks, the unprovisioned journey refuses at the same head. The 1614 residues
in the same block (empty namespace, fromLatDeg0 copy chain) died with it.