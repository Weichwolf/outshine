Type: task
Area: generators
Tags: scope
Parent: 0148

**The served `bridge` and `tunnel` booleans consumed**

The served `bridge` and `tunnel` booleans consumed — they are in the streets and water_lines layers and nothing reads them

---

**Closed, folded into the hull (2026-08-22):** the task lives as a checkbox in 0148; it is refiled as its own item the day work on it begins.
