Type: feature
Area: corpus
Tags: khronos, core

**Suzanne is green on both counts**

*Suzanne* -- tagged `core`, `testing` at the pin, published as `glTF`.

**Criteria met and the picture within the bound**, on the first run.

| case | criteria | picture bound | failing metrics |
|---|---|---|---|
| `test/khronos/glTF/Suzanne` | met | within | none |

Blender's chimpanzee, and the first fetched subject in this corpus whose silhouette turns continuously over a thousand triangles. Its textures and its metallic-roughness row are NOT what this case decides, and the manifest says so.

**It is `emission-per-material`, keyed by the name the FILE gives its material**, so the case states that
the material row reached the body. The colour is the manifest's and carries no claim about the asset's
own appearance.
