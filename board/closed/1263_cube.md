Type: feature
Area: corpus
Tags: khronos, core

**Cube is green on both counts**

*Cube* -- tagged `core`, `testing` at the pin, published as `glTF`.

**Criteria met and the picture within the bound**, on the first run.

| case | criteria | picture bound | failing metrics |
|---|---|---|---|
| `test/khronos/glTF/Cube` | met | within | none |

A solid with per-face normals. **The faceting is not what this decides** -- under a uniform environment an unoccluded diffuse face returns `rho*L` independent of its normal, so a lit arm would render every face of a convex body the same colour. What is decided is the silhouette and the material's binding.

**It is `emission-per-material`, keyed by the name the FILE gives its material**, so the case states that
the material row reached the body. The colour is the manifest's and carries no claim about the asset's
own appearance.
