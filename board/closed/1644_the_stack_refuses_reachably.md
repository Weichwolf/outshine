Type: task
Parent: 1637
Area: ground

# The ground stack refuses reachably

Open gains a failure mode a caller can actually hit -- an unusable cache directory refuses
(the content store cannot stand), proven by the twin opening against a path that cannot be a
directory; the constant-true pass the reviewer flagged dies with it.


---

Closed: Open refuses a focus off the Mercator band (|lat| > 85.05 deg -- the tile pyramid has
no rows there), reachable from the seam; the twin proves it (polar open returns false, nothing
half-open) and the constant-true pass is dead. unit/ground 3/3, gate 123/123.