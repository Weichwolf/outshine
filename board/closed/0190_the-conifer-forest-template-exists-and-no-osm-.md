Type: task
Area: generators
Tags: scope
Parent: 0127

**The `conifer_forest` template exists and no OSM rule selects it**

The `conifer_forest` template exists and no OSM rule selects it — the served schema has one `forest` kind and cannot tell the two apart. TILE, or an inference from elevation and region


---

**Closed, folded into the hull (2026-08-22):** the task lives as a checkbox in 0127; it is refiled as its own item the day work on it begins.
