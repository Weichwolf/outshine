Type: task
Parent: 1637
Area: ground

# The ground stack opens or refuses, and its twin proves it

Open returns the truth: a failed source registration closes the stack and returns false; the
surface constants carry their origin; the boundary takes string_view; a twin in
test/unit/ground proves the refusal and the open/close cycle in the fast gate.


---

Closed: Open returns one verdict (a failed registration closes and returns false), the surface
constants carry [SET] origins, the boundary takes string_view, and unit/ground now mirrors
src/ground ENTIRE with TheGroundStackOpensOrRefuses proving the open/close cycle, usable
streams, and the doctrine boundary (a declaration is not a fetch -- absence answers at read,
per UncoveredIsUndeclared). Fast gate 123/123.