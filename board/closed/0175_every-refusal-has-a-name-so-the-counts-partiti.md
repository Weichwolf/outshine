Type: task
Area: generators
Tags: scope
Parent: 0127

**Every refusal has a name, so the counts partition the region and a missing tree is attributable**

Every refusal has a name, so the counts partition the region and a missing tree is attributable


---

**Closed, folded into the hull (2026-08-22):** the task lives as a checkbox in 0127; it is refiled as its own item the day work on it begins.
