Type: task
Area: generators
Tags: scope
Parent: 0139

**Every declared species measured by a bench (`make treebench`, the deleted tree bench enumerates the directory rather than listing names, so a form nobody grew cannot look green)**

Every declared species measured by a bench (`make treebench`, the deleted tree bench enumerates the directory rather than listing names, so a form nobody grew cannot look green)

---

**Closed, folded into the hull (2026-08-22):** the task lives as a checkbox in 0139; it is refiled as its own item the day work on it begins.
