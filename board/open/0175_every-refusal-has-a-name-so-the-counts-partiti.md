Type: task
Area: generators
Tags: scope
Parent: 0127

**Every refusal has a name, so the counts partition the region and a missing tree is attributable**

Every refusal has a name, so the counts partition the region and a missing tree is attributable
