Type: task
Area: generators
Tags: scope
Parent: 0148

**The served `bridge` and `tunnel` booleans consumed**

The served `bridge` and `tunnel` booleans consumed — they are in the streets and water_lines layers and nothing reads them
