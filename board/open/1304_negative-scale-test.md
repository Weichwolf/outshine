Type: feature
Area: corpus
Tags: khronos, core

**NegativeScaleTest is green on both counts, and the work is keeping it there**

*Negative Scale Test* -- tagged `core`, `testing` at the pin, published as `glTF`, `glTF-Binary`.

**Criteria met and the picture within the bound**, measured over the corpus run this item was
written from. It is one of **11 of 148** in that state.

| case | criteria | picture bound | failing metrics |
|---|---|---|---|
| `test/khronos/glTF/NegativeScaleTest` | met | within | none |

**It is not closed, and the reason is the citation invariant.** A closed `feature` must be cited by
something under `test/`, and nothing cites this id yet. *Closing it is a citation and not a claim -- an
item closed without one is exactly what that invariant exists to catch.*
