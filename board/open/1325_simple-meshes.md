Type: feature
Area: corpus
Tags: khronos, core
Depends: 1362

**SimpleMeshes has no case, and that is the whole of it**

*Simple Meshes* -- tagged `core`, `testing`, `written` at the pin, published as `glTF`, `glTF-Embedded`.

**No case declares this model.** It is one of the **118 of 148** in that state, and that population is
the dominant distance between this tree and the finish line `board:1171` sets -- larger than every red
case and every unimplemented extension put together.

**A case is a directory and the `.gltf` is the declaration**, so adding one is not a writing task. It is
worth adding only where a render can prove something, which is what `board:0079`'s ordered sequence
decides. *This item is the inventory entry for one model; it is not a dispatch and it schedules nothing.*

## The case was written, refused, and withdrawn rather than committed half-built

**Neither material vocabulary reaches this asset.** `emission` keys a per-node colour by the FILE's node
name and this file names no node -- our side refuses with `NOTHING-TO-COMPARE` while the oracle answers
happily to Blender's synthesised `Mesh_0` and `Mesh_0.001`. `diffuse` needs no key and the preparer
refuses it for a different and correct reason: `material.kind is diffuse over 2 meshes, and the closed
form holds for a single unoccluded facet`.

**`board:1362` is the defect and it is not this model's.** Two nodes on one mesh is the smallest
arrangement in the index that draws a mesh twice, which is exactly why it found this first.

