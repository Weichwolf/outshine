Type: task
Area: generators
Tags: scope
Parent: 0146

**One base per building**

One base per building — `FeatureTop` takes the ring's lowest corner, `Buildings::At` derives it from the bbox centre, so a queried prism floats ≈1.5 m against the drawn one on a 10 % slope
