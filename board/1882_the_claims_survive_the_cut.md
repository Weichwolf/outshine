Type: bug
State: active
Area: test
Tags: gate, claims, measured, regression

# Three of twenty-five claims are red, and none of them aborts

Measured 2026-08-25 at 235e3f47, `test/run.sh` in its own worktree, claims section complete:
**22 PASS, 3 FAIL, 0 SIGNAL, 0 BUILD.** The nine that were debris of the cut are repaired and
`EveryNodeTheMapDrawsIsNamedByAProof` is deleted, argued below. What stands:

| claim | what it names |
|---|---|
| `TheBuildDeclarationAuditsItself` | **FAIL x5** — the audit itself is clean (`150 source(s) reach the archive, 0 do not`); it is the four NEGATIVE CONTROLS that fail, every one of them seeding a string the declaration no longer contains (`src/scene src/scene/Store.cpp`, `Wayfinding.cpp src/engine/Sim.cpp`, `src/ground src/engine/Sim.cpp`). A control written against a frozen listing asserts OUR shape, which is why it broke when the shape moved |
| `TheMapCitesLinesThatSayWhatItClaims` | **FAIL** — 2 drifted citations into `Engine.cpp`, both from this hour's own edits. The review owns the repair and made it |
| `EveryOracleWasPreparedByThisPreparer` | **FAIL** — board:1886 owns it: six preparer digests over one corpus |

**`EveryNodeTheMapDrawsIsNamedByAProof` is WITHDRAWN, not repaired,** deleted in 3786fdbb. Its
sentence was *"the unit mirror IS the layering proof, so a class the tree draws in its own
architecture and no test ever mentions is a layer nobody has checked"*. That is a claim about OUR
OWN SHAPE, which the tree no longer accepts: a case declares what the engine stands up and
compares it against a reference whose truth does not depend on our design. It was satisfiable
only because `test/unit/` mirrored `src/` file-for-file. Deleting it is the correct answer; it
stands here so the deletion is a decision and not an omission.

## What will be true

- [x] No claim aborts. A path that is not there is a REFUSAL with a name, never an exception out
      of `std::filesystem` (board:1857).
- [x] `TheSourceCarriesNoCommentary` and `TheDeviceLeavesTheLibraryOnlyForItsOwnTwins` walk
      `src/`, `include/` and `apps/` — the three trees CLAUDE.md names — and are green.
- [ ] Every negative control in `TheBuildDeclarationAuditsItself` seeds against something the
      declaration CONTAINS at the moment it runs — the control finds its own seed site by
      reading the listing, never by quoting a path from the hour it was written. A seed that
      does not take fails the claim as a STALE CONTROL, by that name.
- [x] `EveryNodeTheMapDrawsIsNamedByAProof` is deleted, and the report says so.
- [x] The five BUILD failures compile or are deleted with what they proved.
