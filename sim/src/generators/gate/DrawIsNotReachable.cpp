/* THE GATE, and it must FAIL to build. Occupancy is the server target's; draw is not, and the
 * include set is what says so. */
#include "DrawSink.h"

namespace outshine::Generators {

DrawSink *Forbidden();

} // namespace outshine::Generators
