# --- build outputs (make wasm / walk / world) ---
build/
web/gpu.js
web/gpu.wasm
web/*.data

# --- vendored / generated, recreated by vendor/*.sh ---
vendor/dawn/
vendor/.compat-headers/

# python bytecode
__pycache__/
*.pyc
web/mods/

# --- the browser proof harness's own dependency (tools/watch_browser.cjs); not a build input ---
node_modules/
package.json
package-lock.json

# --- run artefacts (declared runs, logs from fb-sim) ---
runs/
logs/
