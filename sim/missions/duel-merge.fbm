name duel-merge-1
timeout 300

# THE ASYMMETRIC DUEL, GEOMETRY 6: THE MERGE — the one place the MiG-29's weapon set is not the
# inferior one, and therefore the mission that has to exist before the campaign may claim the F-16
# dominates. The four BVR geometries all turn on radar reach and Rtr, and on both the MiG is behind by
# construction (2.0x and 1.7x, measured). Inside the merge neither matters: an R-73 is cued off the
# helmet, a GSh-301 round carries 2.7x the energy of an M61A1 round, and the airframe out-turns and
# out-rolls the F-16 (measured corner 420 kt / 24.2 deg/s against 380 / 16.2; roll plant 201 deg/s per
# full deflection against 78.7).
#
# THE SETUP: 8 km apart, head-on, co-altitude, both on `set task bfm` — the close-combat control law
# on both sides, no BVR phase and no long shot. Both start inside every launch zone either side has.
#
# THE MiG's RADAR IS IN RAD, NOT IN A CLOSE-COMBAT MODE, and that is this aircraft's own constraint
# rather than a setting: the N019's ACM patterns are PENCILS in azimuth (+-1.75 deg CC, +-1.5 VS,
# +-1.25 BORE, sensors.md §4.9), so a manoeuvring fighter cannot acquire with them. RAD is +-30 deg
# and finds him, but it does not auto-lock, so the pilot has to designate with his thumb over the
# command bus (pilot/FBPilot::BfmDesignate). The F-16 is in ACM HUD, which auto-locks.
#
# READING RULE: exit 3 (TIMEOUT, a DRAW), and it is now a real merge duel rather than a departure
# reproducer. The MiG's close-combat DEPARTURE is closed (doc/duels.md D1, this round): it flies the
# whole merge without a flight-monitor KO (aoaMax 24.6 deg, under the 26 deg SOS limit). What the run
# now measures is the OUTCOME once both airframes survive:
#   VIPER (F-16): lock_s 298 / ctrl_s 78 — it DOMINATES the angles, auto-locking (ACM HUD) at t=1.9 and
#     living in the control position, but on an AIM-9-only WVR loadout it never gun-tracks and never
#     converts the angle advantage into a kill.
#   FULCRUM (MiG): survives the whole run flying its cold-search law (search the whole time, lock_s 0) —
#     the nose-on high-closure pass and the turning defensive fight keep the F-16 out of the N019's
#     +-30 deg az / +-6 deg el RAD bar, so the MiG never acquires and never gets an R-73/GSh-301
#     solution. That is the OTHER half of gap 4h (acquisition), now the sole blocker after the departure
#     half was fixed.
# So the merge is a DRAW for a NAMED reason: neither side converts. "The MiG-29 wins the merge" stays
# untested — but the blocker moved from "the airframe departs" to "the MiG cannot yet acquire in a
# turning merge with RAD", and that is a sensor gap (4h a), not a control-law one. doc/duels.md D1.

unit viper
  team friendly
  module f16
  spawn 46.90000 6.95000 5000 90.0 380
  set gear up
  set fuel_pct 60
  set datalink off
  set fcr_mode acm_hud                      # auto-locks the nearest firm track: no thumb needed
  set fcr_range_nm 10
  set rwr on
  set store 3 aim9
  set store 7 aim9
  set cmds_mode man
  set cmds_program 1
  set cmds_chaff 60
  set cmds_flare 60
  set brief_master_arm arm
  set task bfm
  wp 46.90000 7.20000 5000 380
  objective kill unit fulcrum
  objective survive

unit fulcrum
  team hostile
  module mig29
  spawn 46.90000 7.05500 5000 270.0 380
  set gear up
  set fuel_pct 60
  set n019_mode rad                         # the only pattern wide enough to find a manoeuvring jet
  set n019_range_nm 10
  set n019_emission illum
  set rwr on
  set store 3 r73
  set store 4 r73
  set brief_master_arm arm
  set task bfm
  wp 46.90000 6.80000 5000 380
  objective kill unit viper
  objective survive
