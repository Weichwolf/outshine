name o2-09-exercise-one-1
timeout 500
time 1984-06-14T06:00:00Z

# CAMPAIGN O2, SORTIE 9 OF 10 - PVO INTERCEPT EXERCISE: GROUND CONTROL IN ITS PURE FORM.
# THE EXERCISE, WAVE ONE: FOUR INTERCEPTORS ON TWO SEPARATE GROUND-CONTROLLED VECTORS.
# Spec, anchor and sources: doc/campaigns/o2-pvo-intercept.md. Format: doc/missions/campaign.md.
# CONTROL: none. CHAIN HEAD - `pvo1`..`pvo4` and the `pvo*` complex carry into o2-10-exercise-two.
#
# THE CHAIN, AND WHY IT IS TWO WAVES AND NOT THREE. Sorties 09 and 10 are one roster (`pvo1`..`pvo4`)
# and one defended complex (`pvoews`, `pvocp`, `pvoveh`) flown twice, with `carry units ground stores`
# NOT narrowed: wave two flies with what wave one left. The carry sits here and nowhere else, because
# this is the only place the campaign has a QUESTION about it - IS THE CONTROLLER WORTH THE SAME TO A
# FORCE THAT HAS ALREADY SPENT ITS MAGAZINES AND LOST AIRCRAFT? Ten slots hold one baseline, four
# single-lever variants, one control pair plus its own control run, and this chain; a third wave did not
# fit. WHAT THAT COSTS IS STATED: two points give a DIFFERENCE, never a TREND, so this campaign cannot
# show a force-state curve and does not claim one.
#
# THE CORRIDOR FRIENDLY IS THE T1 ANCHOR ARRIVING AS A UNIT. Mitronin (Warsaw Pact journal 12/1976, read
# in this round) writes that corridors are established through the air-defence zone to guarantee the
# safety of one's own flights, that the enemy may discover and exploit them, and that they must be
# covered by units WITH IDENTIFICATION MEANS. Each wave therefore carries one aircraft on OUR team,
# transponder on, flying inside the raid's piece of sky. It has `objective survive`, its loss is covered
# by nobody, and a fratricide therefore ENDS THE RUN AS A FAIL. That is not a hazard of the design; it is
# the measurement.
#
# THE ARENA, and every coordinate in it is [SET]. An exercise has no battlefield, so this campaign flies
# over the Soviet air-defence firing range at Ashuluk, Astrakhan oblast [T4], taken as ~47.30 N 47.90 E
# and NOT improved on. Flown under `--elev const` on a 0 m datum and with NO TERRAIN MASKING (gap C4).
#   the defended complex   a command post (target_hard) and a vehicle park (target_soft) - the objects
#                          the vector exists to protect
#   the early-warning set  a P-18, `set emcon free`, deployed forward of the complex on the approach
#                          axis. GAP C1 IS CLOSED AND THIS CAMPAIGN RE-CHECKED IT: the spec still says
#                          "target_soft only, the EW radar that generates the vector is inert". It is
#                          not inert any more - it radiates, it is heard by the intruder's RWR as
#                          `kind=surface-search` from t = 0.1, it builds its own tracks, and it can be
#                          killed. Every ground object in this campaign is OURS and none is a target
# Scale at this latitude: 1 deg of longitude = 75 490 m, 1 deg of latitude = 111 132 m [DERIVED].
#
# TEMPORAL HONESTY AND ITS DIRECTION. The PVO's own subject was a large, slow, high, unmanoeuvring
# aircraft - a bomber or an ELINT platform - and FlightBox has neither as a flyable module (gap C7: the
# catalogue's `tu95` row is a KINEMATIC MOVER, not an airframe). The intruder here is an F-16: faster,
# smaller and far harder to find than the aircraft the doctrine was written against. THE SUBSTITUTION
# MAKES THE INTERCEPT HARDER THAN HISTORY, so a FlightBox result in which the loop still closes says
# something WEAKER than the doctrine claimed, and one in which it does not close says nothing about it.
#
# AND THE CATALOGUE IS NOT A WAY ROUND IT. Four `air` rows are ACCEPTED, and ACCEPTED in
# doc/modules/air/flight-model-recipe.md means ACCEPTED AS A FLIGHT MODEL - the promotion gate measures
# the DECK and nothing else. It measures no weapon, no sensor and no engagement, and four of the ten
# armed rows cannot employ anything at all. A campaign citing a catalogue row as a COMBATANT would be
# citing something no gate has ever checked, so this campaign cites none: every aircraft in all ten
# files is one of the two flyable modules.
#
# THE STRUCTURAL DISCLOSURE, repeated in all ten files because a reader who skips it reads every result
# backwards:
#   1. THE CONTROLLER IS A STRING (gap C6). `set brief_gci` is fixed before the run: nothing re-vectors,
#      nothing goes silent mid-intercept, nothing is wrong halfway through. Every wrong brief in this
#      campaign is wrong FROM THE START, which is the easy case, and the campaign says so.
#   2. THE PILOT CANNOT DISAGREE WITH HIS BRIEF, AND WHAT LOOKS LIKE A RECOVERY IS A DEAD-BAND ACCIDENT.
#      `FBMig29Pilot` types what it was given and has no path to re-enter a call it has reason to doubt.
#      `FBPilot`'s own uncued search law does keep posting an elevation, but only when ITS OWN wanted
#      angle has moved more than `kInterceptElDeadDeg` = 2.0 deg from ITS OWN last command - and that
#      cache is per aircraft. MEASURED in sortie 02: the wingman's cache sat at 0.0, drifted past the
#      band at t = 55.9 and pulled the antenna back down; the leader's sat at -2.0 from t = 3.5, never
#      drifted, and never looked at the raid again. One aircraft of two recovered, 28 s late, and not
#      because anybody doubted the controller. So 02 and 03 measure the cost of a wrong entry AND the
#      absence of any deliberate recovery, and the two cannot be separated inside one file.
#   3. THE GCI ELEVATION ENTRY IS A WORLD-FRAME ANGLE IN A BODY-FRAME COMMAND (doc/pilot.md 2.15, found
#      by O5 and STILL OPEN on the tree this campaign was flown against). Every interceptor in all ten
#      files is therefore laid out in LEVEL flight, and what that costs is measured in this campaign's
#      own attribution run rather than argued (see doc/campaigns/o2-pvo-intercept.md, State).
#   4. `FBMissionRunner` ends the whole run at the first flight-monitor K.O. (`FirstFlightKo`).
#
# THE ONE TACTICAL QUESTION: DOES THE GROUND PICTURE AND THE AIRCRAFT PICTURE EVER AGREE, and when
# they disagree, which one does the pilot fly? Two flights are vectored from two directions onto a raid
# that contains one aircraft of our own. The controller's two briefs are correct about the RAID and say
# nothing at all about the corridor traffic inside it - because `set brief_gci` carries four numbers and
# none of them is an identity. The interceptors have exactly one way to tell the difference and it costs
# them their surprise.
#
# THE SECOND FLIGHT IS STAGGERED IN TIME (first call at t = 35 against t = 20) and enters from the north
# at 58 km, which is beyond the N019's 50 km gate: its brief aims an antenna at something it cannot yet
# see. That is the doctrine's own shape - the controller sees further than the fighter - and it is what
# separates the two engagements in TIME as far as one file can, given that `FirstFlightKo` ends the run
# at the first wreck.
#
# READING RULE (binding): exit 1 or 3 are both expected shapes and NEITHER is the verdict. Read:
#   1. both flights' `gci` chains and their `radar RADAR_CONTACT` - the campaign's standing measurement,
#   2. `radar IFF_REPLY unit=pvo* ... reply=friendly` - whether anybody interrogated the corridor,
#   3. `sms RELEASE`, `stores DETONATION` and `damage KILL` - who shot what,
#   4. `mission UNIT_RESULT unit=w1c` - a FAIL here is a FRATRICIDE and is the file's most important line,
#   5. `mission OBJECTIVE ... deny release team friendly` and `sms RELEASE ... store=mk84`.
# Whatever this wave spends and loses is wave two's initial condition, and the `campaign CARRY` lines of
# step 10 are where that becomes visible.
#
# MEASURED (fingerprint e915620d3bd44811, exit 3; identical standalone and as campaign step 9, because
# nothing is carried IN to the chain head): THE CORRIDOR SURVIVED, AND THE RAID WAS DENIED BY A HIT
# THAT DID NOT KILL.
#   THE TWO VECTORS. sok1: BRAA t=19.9 -> EMISSION t=27.9 -> first raid track t=30.9 at 16.09 nm.
#   sok2, staggered and entering at 58 km - beyond the N019's 50 km gate: BRAA t=34.9 -> EMISSION
#   t=42.9 -> first raid track t=45.9 at 16.77 nm. The controller's reach is larger than the fighter's
#   and the brief aims an antenna at something that is not there yet; both flights still take 8.0 s and
#   3.0 s, so the loop's cost is a property of the cockpit and not of the range.
#   THE DENIAL WAS EARNED INSIDE THE SIMULATION. 10 releases (7 R-27R, 3 R-73), 4 detonations, no kill.
#   `w1a` took three arrivals - 12.38 m and 7.13 m in the NOSE, 9.26 m in the FORWARD zone - and its
#   `stores` system FAILED at t=128.3. It flew on, reached its aim point and released nothing.
#   `deny release team friendly` MET on all four interceptors; `kill team friendly` unmet on all four.
#   THE CORRIDOR AIRCRAFT WAS INTERROGATED AND ANSWERED. `w1c` returned `reply=friendly` to pvo2 at
#   t=30.9 (34.1 km), to pvo3 at t=45.9 (47.4 km) and to pvo4 at t=48.9 (47.6 km). No round was fired
#   at it and it recovered: `mission UNIT_RESULT unit=w1c result=SUCCESS`. NO FRATRICIDE - and the [T1]
#   anchor's own condition ("covered by air defense units WITH IDENTIFICATION MEANS") is the one the
#   fighters met and a FlightBox battery structurally cannot.
#   THE HONEST CAVEAT ON THAT SENTENCE: attributing a `reply=friendly` to `w1c` was done BY THE ANALYST,
#   after the run, out of two telemetry files. The simulation contains no contact-to-unit correlation
#   and must not; the log gives a track number and a range, never a name. What is measurable INSIDE the
#   run is that a friendly reply existed and that no weapon went that way.

unit pvo1
  team hostile
  flight sok1 1
  module mig29
  spawn 47.30000 48.20000 8000 270.0 450
  set gear up
  set fuel_pct 70
  set n019_mode rad
  set n019_emission off
  set rwr on
  set iff_interrogator on
  set kols_mode ir
  set brief_sort left
  set store 3 r27r
  set store 4 r27r
  set store 5 r73
  set store 6 r73
  set brief_master_arm arm
  set task intercept
  set brief_gci 20 250 38 8
  set brief_gci 50 250 25 8
  set brief_gci 80 250 12 8
  wp 47.30000 47.60000 8000 450
  objective kill team friendly
  objective deny release team friendly
  objective survive

unit pvo2
  team hostile
  flight sok1 2
  module mig29
  spawn 47.31200 48.21500 8150 270.0 450
  set gear up
  set fuel_pct 70
  set n019_mode rad
  set n019_emission off
  set rwr on
  set iff_interrogator on
  set kols_mode ir
  set brief_sort left
  set store 3 r27r
  set store 4 r27r
  set store 5 r73
  set store 6 r73
  set brief_master_arm arm
  set task intercept
  set brief_gci 20 250 38 8
  set brief_gci 50 250 25 8
  set brief_gci 80 250 12 8
  wp 47.30000 47.60000 8000 450
  objective kill team friendly
  objective deny release team friendly
  objective survive

unit pvo3
  team hostile
  flight sok2 1
  module mig29
  spawn 47.62000 48.30000 8000 250.0 450
  set gear up
  set fuel_pct 70
  set n019_mode rad
  set n019_emission off
  set rwr on
  set iff_interrogator on
  set kols_mode ir
  set brief_sort left
  set store 3 r27r
  set store 4 r27r
  set store 5 r73
  set store 6 r73
  set brief_master_arm arm
  set task intercept
  set brief_gci 35 221 58 8
  set brief_gci 65 215 45 8
  set brief_gci 95 205 33 8
  wp 47.40000 47.80000 8000 450
  objective kill team friendly
  objective deny release team friendly
  objective survive

unit pvo4
  team hostile
  flight sok2 2
  module mig29
  spawn 47.63200 48.31500 8150 250.0 450
  set gear up
  set fuel_pct 70
  set n019_mode rad
  set n019_emission off
  set rwr on
  set iff_interrogator on
  set kols_mode ir
  set brief_sort left
  set store 3 r27r
  set store 4 r27r
  set store 5 r73
  set store 6 r73
  set brief_master_arm arm
  set task intercept
  set brief_gci 35 221 58 8
  set brief_gci 65 215 45 8
  set brief_gci 95 205 33 8
  wp 47.40000 47.80000 8000 450
  objective kill team friendly
  objective deny release team friendly
  objective survive

unit w1a
  team friendly
  flight raid1 1
  module f16
  spawn 47.15500 47.61500 8000 50.0 450
  set gear up
  set fuel_pct 80
  set fcr_mode off
  set rwr on
  set datalink on
  set store 3 mk84
  set store 7 mk84
  set brief_master_arm arm
  set task attack
  set attack_mode ccrp
  wp 47.67500 48.53000 8000 450
  objective kill unit pvocp

unit w1b
  team friendly
  flight raid1 2
  module f16
  spawn 47.13432 47.64054 8150 50.0 450
  set gear up
  set fuel_pct 80
  set fcr_mode crm
  set rwr on
  set datalink on
  set store 3 aim120
  set store 7 aim120
  set brief_master_arm arm
  set task intercept
  wp 47.70000 48.60000 8150 450
  objective survive

unit w1c
  team hostile
  module f16
  spawn 47.19000 47.56000 7800 50.0 450
  set gear up
  set fuel_pct 80
  set fcr_mode off
  set rwr on
  set datalink off
  set iff_xpdr on                           # THE CORRIDOR: the only thing that says it is ours
  wp 47.70000 48.48000 7800 450
  objective survive

unit pvoews
  module p18
  team hostile
  spawn 47.55000 48.20000 ground 0.0 0
  set emcon free

unit pvocp
  module target_hard
  team hostile
  spawn 47.67500 48.53000 ground 0.0 0

unit pvoveh
  module target_soft
  team hostile
  spawn 47.68500 48.52000 ground 0.0 0
