{
  "name": "flightbox-cc",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "FlightBox headless Command Center — a TypeScript client of the flightbox hub (MSP over the multiplexing proxy) + a declarative mission/test harness. Zero runtime deps (Node built-ins only); built with the system tsc (Debian node-typescript).",
  "bin": { "cc": "dist/bin/cc.js" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "cc": "node dist/bin/cc.js",
    "test": "node dist/bin/harness.js"
  }
}
