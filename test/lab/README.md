# test/lab -- where a non-trivial answer is PROVED before it is implemented

**Python proves correctness; the C++ in `src/` makes it real-time.** A structural or numerical
question that cannot be answered by reading a reference is answered here first: the inputs are
the engine's own (the same OSM, the same DEM tiles, decoded by numpy), the solution is written
with the scientific libraries (numpy, scipy, shapely, trimesh, networkx, osmnx), and a PROOF is a
check that goes red -- a constraint violated, a residual above tolerance, a negative control that
passes. Only then is the algorithm written in C++, and the lab's result is the oracle the C++
answer is measured against.

Environment: `python3 -m venv test/lab/.venv && test/lab/.venv/bin/pip install numpy scipy shapely
matplotlib trimesh mapbox_earcut networkx pillow osmnx`. Data lands under `$TMPDIR/outshine-lab/`
and never in the tree. Run an experiment as `test/lab/.venv/bin/python test/lab/<dir>/<exp>.py`.

## Inventory

| experiment | question | solution | proof | to C++ |
|---|---|---|---|---|
| `roads/profile.py` | what does the DEM say along every OSM way, and where are the outliers? | sample terrarium z14 at every node, grades per class | measured 2026-09-05 at OldTown: 3 340 ways; steepest 0.568 on a path, secondary 200 of 910 segments over its 7 % (94 of them under 12 m long) -- the outliers are aliasing of a coarse DEM against short segments, plus real steps | the measurement itself is `Path::Network::Elevate`'s (board:2101) |
| `roads/band.py` | the road profile: one height per OSM node, smooth where the DEM cannot resolve, faithful where it can, a bridge's deck free of the river bed | a QP (cvxpy/OSQP): minimise `sum w (z-dem)^2 + l^4 sum kappa^2` with `w = 0` on a bridge's or tunnel's interior nodes, `l = 2 DEM postings` (12.4 m at zoom 14, 49 N), the band `|z - dem| <= 4 m` hard [SET: Copernicus GLO-30 LE90 < 4 m], the class's design grade a soft slack that is REPORTED, never forced (netconvert's warning) | measured 2026-09-05 at OldTown: band met to 1e-9, curvature RMS 0.0095 -> 0.0013 1/m, `|z - dem|` max 4.15 m (a deck), grade p99 0.248; controls: the band shut returns the DEM to 1e-5, an isolated 2 588 m way with the band open fits a line (curvature 1.2e-6); with `l` = 0 the hilltops are cut to the band, which is why `l` is the DEM's and not a taste | the normal equations, see the next row |
| `roads/band_iter.py` | the same profile as a C++ can compute it, deterministically; measured at Heidelberg too: the direct solve equals the QP to 0.87 mm; with a grade-fidelity term (mu = l^2) added, the active set of a HARD band oscillates at Heidelberg, and the band model comparison (fixed / slope-scaled / none) gives the same profile to the millimetre at both places -- so the band goes and the direct solve is exact by construction. A C++ port was written and taken out again the same day: on the engine's own graph (versatiles ways, points snapped to nodes two metres apart) it read 30 m/m from a centimetre of station and 1e14 from a deck component with no abutment -- the cases a synthetic bed has to hold BEFORE the port (`roads/synthetic.py`, next) | the normal equations `(W + l^4 K^T K) z = W dem` by a sparse DIRECT solve (scipy SuperLU here, Eigen's SimplicialLDLT in C++), the band by an active set of at most 8 rounds | measured 2026-09-05 at OldTown: the direct solve equals the QP to 0.00000 m with no bound active in one round, 0.04 s for 12 552 nodes; Jacobi-preconditioned CG is NOT the tool: 1 024 iterations still 0.31 m off, the system's condition is `l^4 / ds^4` | `Path::Network::Elevate` (board:2101): a sparse LDLT over the node graph |
| `roads/CASES.md` + `roads/synthetic.py` | every case of infrastructure x terrain the planet has, in isolation, as MAP and as MESH: the bed builds terrain x network, snaps and dedupes as the weave does, solves the map, meshes the legs and the junction regions, and checks I1-I9 | the map as in band.py plus the junction and ramp rules; the MESH: a ribbon per drawn span with a vertex at every break of the cross-section (the crown) and TWO rows at every polyline vertex (one per segment, the wedge between them closing the corner), the station step from the chord's sagitta per segment (L = sqrt(8 tol / k)), the junction REGION (its polygon plus every minor leg's warp band) meshed on a grid whose cell follows from the crossfall and the tolerance, all welded on a quantised key | measured 2026-09-05: 76 of 76 green -- the drawn surface stands within 1e-14 m of the analytic one on flat ground, 0.16 mm over a crest, 6.9 mm over a DEM cliff, 5 mm on noise; no edge carries more than two faces; the roundabout is one junction (netconvert's node joining, the ring whole, its surface an annulus). Four defects the mesh check found and named: a forward tangent collapsed the section at a way's end (3.1 cm), a mitred row spans two directions and no subdivision shrinks its twist (4 cm = grade x offset x tan(theta/2)), one curvature for a whole way took the step from its flattest stretch, and a way drawn through a junction put two surfaces in one place | nothing yet: joining, then the tile seam and the ground's cut and fill, then `Path::Network` and the structure stage (board:2101) ; and two products beside the map: the EARTHWORKS (batter at 1:1.5 filling and 1:1 cutting, the toe by a root find because the batter's height changes with the terrain, a retaining wall above 3 m) and the SEAM (a tile's own solve against the whole's). Measured: the seam's error falls like exp(-halo / l) with l the smoothing length -- 411 mm with no halo, 21 mm at one l, 4.8 mm at two, 1.3 mm at four -- so a tile must fetch TWO SMOOTHING LENGTHS (four DEM postings, 100 m at zoom 12) of neighbouring network or its roads cannot meet, and no welding afterwards repairs it |
| `buildings/synthetic.py` | an OSM footprint plus its tags plus the DEM, made into a CLOSED body that stands on the ground | the roof is a DISTANCE FUNCTION and nothing else: a hipped roof of equal pitch is z = tan(pitch) x dist(p, boundary), whose ridge set is the polygon's straight skeleton and whose level sets are its inward offsets, so the mesh puts a vertex on every ridge by sampling those offsets and bisecting for the last one; a gable clamps the distance across one axis, a pyramid normalises it, a skillion is one plane, a mansard two pitches in series, a dome a circle over it. The wall's TOP follows the roof's edge (a gable end rises to the ridge) and its FOOT follows the ground, buried by 0.5 m [SET]; the pad is the footprint's HIGHEST ground corner, so a building cuts into the hill and never floats, and over water it stands a freeboard above it | measured 2026-09-05: 35 of 35 cases green -- seven footprints (rectangle, L, U, courtyard with a hole, round, thin, tower) on five grounds (flat, 15 %, 40 %, terraces, water) with seven roof shapes; every body watertight (0 open edges, 0 edges over two faces), positive volume, no gap between wall and ground, and every ridge within 5 cm of its own arithmetic (tan(pitch) x inradius, the inradius found by bisection on the inward offset -- a 40 x 40 grid read it half a cell short and the apex 0.13 m low) plus the FACADE as a grammar: bays that divide each wall exactly, storeys from `building:levels` (OSM's rule: `height` is the whole building, levels count the storeys under the roof -- reading it wrong left a three-storey house with one row of windows), an entrance in the middle bay of the street side, a French door wherever a balcony stands, and a blind bay where the roof cuts the cell. Four rungs: L0 the mass, L1 the same geometry with the facade as material parameters, L2 the reveals and the cornice, L3 the balconies -- monotone and checked, with L1 costing no geometry so it shares L0's draw. Every case is drawn as a SHEET (plan with poched walls and dimensions, two elevations with the roof's silhouette and the ground line, a section through the ridge) | nothing yet: the building stage in C++ |
| `roads/real.py` | does the ROAD bed survive a network somebody surveyed, and can the hardest structures on this planet be rebuilt from OSM alone? | a LADDER of thirty places in five rungs -- one clean structure (Golden Gate, Muengsten, Ponte Vecchio, Forth, Glenfinnan, Goeltzschtal, Millau, Tower Bridge, Hohenzollern, Landwasser, Brusio, Storseisundet), then a junction (Spaghetti Junction, Pregerson, Kaiserberg, Magic Roundabout, Etoile, Puxi), then a mountain (Stelvio, Lombard, Transfagarasan, Furka), then rail (Zuerich HB, Semmering, Schwebebahn, Hamburg Hbf), then the vertical stack (Elbtunnel, Wacker Drive, Yerba Buena, Hong Kong Central) -- each through the SAME bed as `synthetic.py`, with the axis SEEDED by the way the case is named after | measured 2026-09-05: four defects real data found in the bed within the first case. `_snap` was O(N^2) and a real extract carries ten thousand nodes, so it is a grid now with the same declared order. A way that COLLAPSES to one node under snapping was carried to `centreline`, which asked shapely for a LineString of one point; they are dropped once, in the snap. `_direction` used a Python negative index, so at k = 0 with sgn = -1 an open way read its own last node and a CLOSED way (a roundabout, a turning circle) gave a zero vector and divided by it. And the sheet seeded its route from the LONGEST way, which in a city is whatever street happens to be longest -- the Ponte Vecchio's sheet was a section along a footpath **WHERE THE LADDER STANDS, measured 2026-09-06: TWENTY-SIX of thirty fully green.** Four were green when the ladder was built and all thirty were red when it first ran. I6 and I2 are gone from every case but Zuerich HB, and I3 from every case at all. The four that remain, each with its family, its number and now its WAY: the RAMP's grade over the terrain at Puxi (0.43 m) and Hong Kong Central (3.32 m); the DECK's own grade at Wacker Drive (0.56 m, and the per-way pose names a 68.5 m `secondary` with no bridge tag, which the per-family pose does not -- the two instruments have to be unified before either is quoted); and I6 at Zuerich HB, 1 854 ways, 5 763 nodes, 120 tunnels, layers -5..2, the densest case on the ladder. | the typology and the snap belong to `Path::Network` (board:2101) |
| `buildings/real.py` | does the rule survive the DATA? the same bed on fifteen real landmarks, fetched from Overpass by coordinate, with the ground the engine's own DEM gives | the largest building way that CONTAINS the point (the biggest way in reach at the Chrysler is the block it stands in), its `building:part` ways as parts, and OSM's own spellings cleaned into numbers: `height=157 m`, `building:levels=5;6`, `start_date=C13`, `roof:shape=round` | measured 2026-09-05: the ROOF had to become a CONSTRAINED triangulation before any of them closed -- an unconstrained Delaunay clipped by a centroid test cuts corners and leaves holes where a footprint is narrow (46 open edges on the Reichstag's 15-node outline), and Shewchuk's `triangle` with the rings as segments closes the body by construction. The WINDING is the tree's and not the source's: a clockwise OSM way turned the Koelner Dom inside out, so it is normalised in the constructor. Three classification defects the real data found and a synthetic case never would: a cathedral read as an office tower until a named use beat the height test; a 64 m Bundeshaus over a 62 m footprint read as a curtain-walled tower until a tower had to be SLENDER (h > 2.2 sqrt(A)); and a domed civic building with no `start_date` read as `late20` until the dome was allowed to date itself. SIMPLE 3D BUILDINGS then settled the heights: where `building:part` ways cover the outline, THEY carry them and the outline's `height` is the tallest point of the whole -- extruding the Koelner Dom's entire 146 x 87 m plan to its 157 m towers drew a box the size of the spires, and the Empire State's 443.2 m is its mast. The mass takes the LOWEST part (the aisle, 15 m) and every part rises out of it, and parts are kept for their HEIGHT before their area or a cathedral comes out with no spires. Three more selections the real data forced: the NAME wins before the geometry (the biggest way holding the Sagrada Familia's coordinate is an apartment house), the `building` VALUE wins where OSM gives the landmark no name at all (Zurich's station is an unnamed `building=train_station` RELATION under a 44 000 m2 platform roof), and a relation's outer members have to be CLOSED again before `polygonize` sees a ring -- `as_metres` drops the repeated node, so the station produced no polygon at all. All 15 now select the building they are named after | with `buildings/synthetic.py`: the building stage in C++ |
| `buildings/synthetic.py` sheets | do the drawings pass as BUILDING PLANS? | A2 landscape (594 x 420 mm), a Schriftfeld where DIN 6771 puts it, and every panel's scale MEASURED off its finished axes and then made true -- the limits are set to exactly what the declared scale spans, so a ruler checks it | measured 2026-09-05, looking at every sheet as the engineer: a roof drawn as a grey band with no eaves and no ridge; windows with no reveal, sill or head, so a wall had no thickness; a Kranzgesims as one stripe where the period has four courses; a gothic wall with rectangular windows and rectangular buttresses; a cathedral section with no vault, and then a vault rising through the roof because nothing said a crown stands under the eaves; chimneys on a baroque CHURCH (the rule read the epoch, not the use) and two more hanging off a gable end beside dormers, because neither asked whether a roof SLOPE faces the viewer; a 34 m onion over a nave; and the Zwiebelhaube itself drawn as a cone | the elements are the L2/L3 rungs' specification |
| `roads/synthetic.py` bridge typology | what a bridge IS when the data says nothing | a SURVEY beats a HEURISTIC: `bridge:structure` and `material` decide outright where OSM carries them. Where it does not, the DATE still speaks -- wrought iron reached bridges about 1850 and reinforced concrete about 1900, so `historic=bridge` or a `start_date` before 1890 is a MASONRY ARCH, there was nothing else to build from. And a free span is a MODERN constraint that a masonry bridge ignores: an arcade puts its piers IN the river. What bounds a stone arch is its RISE (a ring flatter than span/12 is at the limit of what was ever built) and the material (past 40 m is a record, not a type), so the field count follows from those two | measured 2026-09-05 at the Ponte Vecchio, which carries `bridge=yes`, `historic=bridge` and nothing else: a prestressed concrete beam of 2 x 51 m before, a Steinbogenbruecke of 3 x 34 m with d = 1.31 m after, and the drawing is three rings on piers with filled spandrels instead of one 102 m span. The arcade is what every old crossing on the planet is, and drawing it as one span makes every old bridge look modern | the structure stage (board:2101) |
| `roads/synthetic.py` I12, the clearance fixed point | does the loop that lifts decks over the roads below them CONVERGE? | it is a SUBSTITUTION -- each round re-reads the floors from the roads as they now stand -- and nothing proves it contracts. Two fixed rounds were an assumption; it now iterates to a tolerance (1e-4 m, bounded at 40) and REPORTS the residual, and I12 goes red when the residual stands | measured 2026-09-06: it DIVERGES on real mountain roads. At the Transfagarasan the rounds move the profile 24.4 m, then 11.5 m, then 9.1 m; forty rounds leave 0.44 m and the profile walks to 105 m off the DEM. At the Stelvio forty rounds leave 4.44 m and 183 m. Millau converges in ONE round. Two fixed rounds hid it: they stopped before the walk was visible and the bed called the result an answer, which is why the band read 37.4 m and looked like a smoothing problem. **Three hypotheses tested and REFUTED, each by a case that came out green:** the node spacing is not it (`R2-hairpin-dense`, the same serpentine noded every 4 m instead of 25, reads 2.27 m); the smoothing length is not it (at l = 0.25 postings the band is still 17 m); and a deck over another deck's RAMP is not it (`R11-rampcycle`, on the flat and on a 15 % cross slope, converges to 1e-9). What the divergence needs is still unnamed, and the answer when it is named is a STRUCTURE -- all the clearance inequalities in ONE constrained solve, the way `band.py` posed the band -- not a longer loop A FOURTH HYPOTHESIS, and this one was a real defect: `_clearances` skipped every way that SHARED A NODE with the deck. The rule was meant to exclude the deck's own APPROACHES and it excluded the thing the clearance exists for -- on a spiral, a hairpin or any interchange the road passing underneath is usually the SAME road that leads onto the deck, joined at the abutment. `R11-spiral` was added to reach it (a road that crosses itself, which is what the Brusio viaduct is and what every hairpin with a bridge over the leg below is made of) and it came back with `clearance_m` = 1e9, the sentinel for NO ROAD FOUND. What is excluded now is the approach itself: the stations of the other way within 60 m of a shared node. | board:2101, before the structure stage |
| `roads/synthetic.py` I3, the DEM band | how far from the DEM may a road on the ground stand? | not a constant. A BILINEAR interpolant reproduces a PLANE exactly, so a uniform 40 % hillside costs it nothing; what it cannot represent is a bend smaller than its posting -- a ridge, a cut, a hairpin whose two legs fall inside one cell -- and the error there is about |z''| h^2 / 8. The band is the survey's own error (4 m [SET], Copernicus GLO-30 LE90) plus that term, per node, from the DEM's own second differences. ONE source: the constraint inside the solve, the check afterwards and the elastic diagnosis all read the same array, and I3 is reported as a RATIO where 1.0 is exactly at the band | measured 2026-09-06: Millau falls from RED at 5.45 m to green at 1.00x and the Golden Gate reads 0.19x, with no change to any of the 81 synthetic cases. The band is now also a HARD CONSTRAINT and not only a check, because stating it afterwards let the QP satisfy a clearance by pushing the road BELOW a bridge 17 m into the hill instead of lifting the deck | board:2101 |
| `roads/synthetic.py` the elastic pose | when the constraint set is INFEASIBLE, which constraint and where? | `infeasible` names no place and no constraint, so the same problem is posed again with a non-negative slack on every clearance pair, every band bound and (as one scalar) the whole ramp-and-grade family; the slacks are driven down and what is left is the SMALLEST violation that makes the set consistent, with the node it sits on | measured 2026-09-06: the Stelvio reports `clearance short by 4.80 m at node 11494440080` and the Transfagarasan `band exceeded by 11.47 m at node 10067011076` -- two different defects that both read `infeasible` before. The ramp-and-grade family is built once and used by both poses, or the diagnosis would be about a different problem than the solve | board:2101 |
| `roads/synthetic.py` I6, the junction step | WHAT does the invariant measure? | the step where a MINOR leg ties into the major's surface. The major IS the junction's surface -- RAS-K's and AASHTO's rule that the through road keeps its section -- so comparing it with ITSELF measures the projection and not the road: both sides are `own_surface` of the same way at the same station, and the difference is only how well a nearest-point search recovered a station it was handed | measured 2026-09-06: that artefact read 0.11 m at the Magic Roundabout and 0.10 m at Lombard Street while the surface was continuous by construction, and it is what kept sixteen real cases red. Two real defects were found and fixed on the way there: the leg's cut point was struck in the frame `sgn` asked rather than the WAY's, so at sgn = -1 the normal reversed and a superelevated section was read on the wrong side (0.2100 m = 2 x 6 % x 1.75 m at the Golden Gate, invisible to a crowned section, which is every synthetic major); and the point was struck on a straight TANGENT while the junction found its station by PROJECTION, which are two stations on a curve. **Negative control, RED:** with `leg_surface` returning the unwarped section, I6 reads 0.453 m on a T junction, 0.496 m on an X and 1.440 m on a roundabout, against exactly 0 with the warp | board:2101 |
| `roads/synthetic.py` where a road is UNDER a deck | which roads must a deck clear? | the ones whose RIBBON its own ribbon crosses. A node-distance test can only approximate that, and both errors were made: at half-widths plus 25 m every road running BESIDE a bridge -- the other carriageway of a dual road, the street along a viaduct -- became something to clear by 4.5 m, which nothing can do because they stand at the same height; at half-widths plus 1 m a deck node ten metres from the crossing missed it | measured 2026-09-06: the loose test refused fifteen of the thirty real cases as infeasible, and at eight of them the elastic pose reported a shortfall of 4.4 to 4.8 m, which IS the clearance -- the sign that the pair had no room at all. The tight test turned three synthetic cases red on I4road. The crossing is now asked of the two buffered centrelines, once per way pair, cached, behind a bounding-box gate; the node test only picks WHICH nodes carry it. Glenfinnan and Kaiserberg (407 ways, 52 bridges) went from refused to fully green | board:2101 |
| `roads/synthetic.py` what a deck must CLEAR | how much headroom, over what, and how far does the embankment reach? | 4.5 m is the figure for a ROAD (RAS-Q; AASHTO's 16 ft is the same). A footway or a path needs headroom for a person (2.5 m), a railway needs room for its CATENARY (6.0 m, EBO), and `layer` is the third dimension OSM actually carries -- a way at the deck's own level or above is not underneath it, and a way in a BORE is under the ground and owes a bridge nothing. And an EMBANKMENT does not end where a way does: to gain h metres at the class's grade a ramp needs h/g of length, 4.5 m at 1.25 % is 360 m, and OSM splits a road wherever a tag changes, so the fill crosses several ways. The exemption walks the network with a BUDGET in metres | measured 2026-09-06: asking 4.5 m over a footpath made the Glenfinnan viaduct infeasible by 0.74 m and the Goeltzschtal by 0.96 m, both rail viaducts with a path under their arches; Glenfinnan is green with the right figure. Marking only the ways that TOUCH a deck pinned the second way of every embankment to the DEM band, and the walk now frees 450 of Kaiserberg's 1 567 nodes. **What is still refused and now has a location:** at Kaiserberg a RAIL bridge whose node must clear FOUR motorway carriageways at once, on a DEM that puts deck and motorway both at 33 m because it has never heard of either. A five-level interchange's whole vertical structure has to be invented, and the bed's ramp taper and deck-grade bounds cannot yet reach it | board:2101 |
| `roads/synthetic.py` the infeasible interchange | why does a compact multi-level crossing have NO solution? | a CYCLE, and the elastic pose found it only after its own weights were corrected: the shape family's slack carried a factor of 100, so the pose broke a clearance PAIR every single time and "clearance short by ..." at sixteen real places was a fact about that objective and not about the road. Weighed equally, and split into one slack per KIND of bound, the answer changed completely -- the DECK's own grade at Kaiserberg and the Elbtunnel, the RAMP's grade at the Stelvio and the Ponte Vecchio. The cycle: deck A is asked to clear deck B's RAMP and deck B to clear A's ramp, while each deck sits within DECK_GRADE of its own ramp, which has no solution. A ramp is a STRUCTURE and OSM does not say where it stands | measured 2026-09-06: dropping the constraint wherever the lower side is an approach turned Ponte Vecchio, Kaiserberg (407 ways, 52 bridges) and the Elbtunnel (922 ways, 49 bridges, 52 tunnels, layers -2..3) green -- and broke three synthetic cases, because an overpass over an on-ramp is a real thing and `R11-rampcycle` is exactly it. The distinction is DISTANCE FROM THE ABUTMENT: a ramp is elevated where it climbs and back at grade beyond, and the synthetic crossing stands 160 m out where the interchange's stand inside 60 m. Both are green | board:2101 |
| `buildings/region.py` | WHERE a building stands, as rules rather than as a coordinate | the generator takes an EPOCH and a GEO-COORDINATE. The coordinate decides the snow a roof must shed (EN 1991-1-3's altitude form, zone from latitude) and hence the pitch below which nobody builds; the seismic belt and hence how many storeys of masonry the ground allows before a frame; the vernacular wall; the eaves' oversail; and WHICH EPOCH TABLE applies at all, because `Gruenderzeit` is a word about Central Europe. Seven regions registered, each one predicate and one entry | measured 2026-09-06: Zermatt 5.11 kN/m2 -> 45 deg minimum pitch, stone, masonry to 4 storeys; Koeln 0.51 -> 5 deg, brick, 6 storeys; Kyoto seismic 0.77 -> FRAME, masonry to 2; Chicago frame, gambrel among its roofs. And one defect the model itself found: the altitude term is a MID-LATITUDE term, and applied unconditionally it gave NAIROBI 6.00 kN/m2 of snow at 1 795 m on the equator -- only the height above the winter freezing line counts, 3 500 m at the equator falling to zero by 45 degrees | the C++ `BuildingShape` has no epoch and no coordinate at all; this is what it owes |
| `buildings/roofs.py` | can a roof shape be added without touching a dispatcher? | fourteen shapes, each ONE function of the polygon's distance function, its principal axis or both, registered by OSM's own name. `roof_height_at` is now only the CONTEXT -- distance, inradius, half-widths across and along the axis -- and hands it to the one registered shape. Each row names the C++ `RoofKind` it corresponds to, or `-` where the C++ side has no such shape yet | measured 2026-09-06: 35 synthetic cases and 15 landmarks unchanged through the move. The C++ generator has seven (Flat, Gable, Hip, Shed, Mansard, Sawtooth, Dome); this bed has fourteen, and the seven `-` rows are the list the lab owes it -- pyramidal, half-hipped, gambrel, barrel, spire, onion, butterfly | board:2101, as a refinement of `RoofSurface.cpp` |
| `buildings/features.py` | can a facade element be added without touching a dispatcher? | thirty elements, each one function with the LOD RUNG it appears at -- L0 the mass, L1 the facade as material parameters, L2 the reveals and profiles, L3 the bodies that stand off the wall. The sheet no longer knows what a Kranzgesims is: it hands one context object to `draw_all` and every registered element that applies here draws itself. A specialisation -- a region's own cornice, an epoch's own window head -- is another entry beside the first, guarded more tightly | measured 2026-09-06: a 318-line if-chain inside the drawing became thirty registered functions with no change to any sheet. Adding an element is a function and one line |
| `render.py` | can the lab STAND A CAMERA where the client stands one? | yes: a place, a bearing, a pitch, a field of view -- the client's own terms -- and a flat-shaded picture with a z-buffer and a two-colour sky. FLAT shading by intent: one normal per triangle, so a crack, a fold or an inverted face SHOWS instead of being smoothed away. No Blender and no scene format; the renderer runs in the same interpreter as the generator that made the mesh, which is what makes it a TWIN rather than an export. The sun is astronomy (NOAA's low-precision solar position) and the sky is a gradient -- the atmosphere is the engine's and a lab picture that pretended otherwise would invite a comparison it cannot win | measured 2026-09-06: it drew all fourteen roof shapes and found what no number had. Weather and climate are NOT computed here or anywhere in the generator |
| `buildings/gallery.py` | are all fourteen roof shapes actually CORRECT, looked at? | the thirty-five cases exercise seven; the other seven had never been drawn. This walks the roof REGISTRY -- so a shape added tomorrow appears without anyone remembering -- and asks CLOSED (no edge with one face, none with three), WOUND (every directed edge exactly once with its partner exactly once, which proves the orientation PER FACE where a positive volume only proves it on average) and SNAPPED (no two vertices nearer than the 1 mm weld and unwelded; snapping is the MESHER's job, in `Building.vertex`) | measured 2026-09-06: all fourteen closed, wound and snapped, and the pictures found three things the numbers did not. A camera at 9 m with a 7 degree pitch renders a skillion as a flat box and a sawtooth as one dark face -- both height fields sampled CORRECT -- so the gallery looks down at 26 degrees with a raking sun, because a gallery whose pictures cannot be read is not a check. A COMB of spikes runs along the eaves of every curved roof (barrel, dome, onion, mansard, gambrel, sawtooth): the cause is not sliver triangles (8 of 3 556 under quality 0.05) and not the offset ring's own distance (both tested and refuted); it is that two consecutive level sets carry DIFFERENT POINT COUNTS, so the band between them zigzags and its normals alternate. Stepping the ring ladder in HEIGHT rather than in distance (0.25 m per ring) halves it and does not remove it. **The answer is a structured RING-AND-SPOKE roof mesh instead of a constrained Delaunay over rings, and that is the next rung** |
| `buildings/synthetic.py` B-wound | is the surface consistently oriented PER FACE? | every directed edge (a, b) exactly once and its partner (b, a) exactly once. A positive volume is a GLOBAL test and a handful of inverted triangles cancel inside a sum | measured 2026-09-06, on the check's very first run: the COURTYARD case carried 40 mismatched directed edges while reporting closed and a positive volume. Two defects behind it. The floor was fanned from the polygon's representative point ONCE PER RING -- including the hole's -- so a courtyard got a second floor laid over the solid; it is one constrained triangulation now, with the hole as a hole. And the wall was FLIPPED for interior rings, when `orient(poly, 1.0)` already puts the exterior counter-clockwise and every hole clockwise precisely so that the solid is on the same side of both. 35 cases green with the check in place |
| `roads/synthetic.py` I2, the C1 profile | is the grade continuous across a node? | it is the cubic Hermite's DEFINING property, so a probe at s +/- eps cannot test it -- it tests the probe. At a fixed 1e-6 m into a 2.2 m segment the two heights agree to about 1e-13 and their difference is catastrophic cancellation; widen the probe and it stops measuring cancellation and starts measuring CURVATURE, which is legitimately not zero. What CAN be wrong is the construction: that the polynomial on each side is built from the stored tangent at all. The check now evaluates the profile AT the node from the left segment and from the right and compares both with `slope[way][k]` | measured 2026-09-06: Hong Kong Central read 1.14e-06 against a 1e-6 tolerance at two nodes of 3 373, where a 2.2 m segment meets a 48 m one -- the last red case on the ladder, and a defect of the CHECK. Restated, it reads 6.9e-18. **Negative control, RED: a segment built from the NEXT node's tangent at its start reads 3.4e-02**, sixteen orders of magnitude away |

## The sheets

Every case of both beds is drawn as an ENGINEERING SHEET and the whole set stands under
`build/shots/lab/` as `NN_case.png` (roads) and `BNN_case.png` (buildings). The title of each
carries what it is, where it stands and -- for a building -- its type and epoch.

A road sheet is DIN 1356 / RAS-Q: LAGEPLAN with the alignment, both carriageway edges, the
junction surfaces, the earthwork toes, stationing along the ROUTE, a north arrow and a scale
bar; LAENGSSCHNITT along the whole route with terrain, DEM, gradient, cut and fill hatched
apart, grades in percent, the structures marked with their span division and their piers drawn;
three QUERSCHNITTE with the crossfall, the kerb, the batters and their slopes, the fill and cut
heights and the toe's reach -- and for a deck the superstructure instead, a slab of its span's
depth with parapets, piers and the clearance dimensioned. A building sheet is a plan with the
wall poched and dimensioned, two elevations with the openings, the balconies, the storey levels
and the roof's true silhouette, and a section through the ridge.

The sheets are the review: reading them as an engineer found and fixed a junction shape that
was a triangle pointing away from its minor leg (two opposite legs are parallel and have no
corner between them -- the edge LINES bound the surface instead), a longitudinal section drawn
along one way instead of the route (a bridge is three ways and the section showed a plateau
where the valley is), a 280 m bridge given a single 14 m deep span (it is six fields of 47 m),
a deck's cross section drawn with a 30 m embankment into the river, a building's elevation
with no roof at all (the walk into the building used the outward normal) and gable ends rounded
off by a grid, an industrial hall with a house door instead of a loading gate, and a sheet
whose title block and cross section disagreed about what "Damm" means.

## Open questions -- the labs still to run

Each becomes a row above when its experiment exists. Research briefs with references land in
`research/` first (who has done it and proven it, readable repos, papers, what SDL_GPU reaches).

| area | question | first reference to read |
|---|---|---|
| roads, structure | a road as geometry: lanes from the map's profile with cross-slope, kerbs, junction polygons from the net, seams closed at tile borders, vertices snapped, no cracks; bridges as decks between abutments | CARLA `OpenDriveToMap`/`MeshFactory` (0.5 m vertex distance), SUMO `NBNodeShapeComputer`, OpenDRIVE elevation/superelevation |
| buildings | what CARLA's OSM plugin and blosm do that a plain extrusion does not: facades, roofs, window grids; which of it is CPU geometry and which GPU detail | `research/world.md` |
| every OSM structure | rails, walls, fences, power lines, street furniture -- CPU geometry vs GPU detail per kind | `research/world.md` |
| ground cover | grass, soil, litter, stones, sand, snow at PS4-class budgets: instancing, compute culling, splatting, virtual texturing | Ghost of Tsushima GDC 2021; `research/world.md` |
| water | ocean (FFT), lakes, rivers with flow, shorelines, refraction/reflection through compute + raster | Tessendorf; `research/world.md` |
| clouds and sky | Nubis-class volumetric clouds driven by real weather; Hillaire 2020 sky | `research/world.md` |
| materials | parameterised PBR with weathering, layered, LOD; what Filament does | `research/light-materials-life.md` |
| lighting by the clock | sun, moon, stars by astronomy; exposure in physical units; night from OSM lamps | `research/light-materials-life.md` |
| seasons and place | phenology, snow, wetness from climatology and live weather; signs, driving side, building styles by region | `research/light-materials-life.md` |
| minds | NPC driving (CARLA traffic manager, SUMO, IDM), walking (Recast/Detour, ORCA), flying (JSBSim), LLM-driven agents with replayable event logs | `research/light-materials-life.md` |
| terrain | the lattice's level boundaries, skirts, and a DEM's error band as a first-class number; stitching real DEM levels | Cesium quantized-mesh, CDLOD |
| determinism | a proof that two runs of one declaration give one byte stream: where threads join, in what order; a fuzz over scheduling | RAGE replay, Unreal automation screenshots |
| physics | vehicles on the analytic driving surface: Jolt's wheeled vehicle, contact with the profile, sub-centimetre agreement between the map and the structure | Jolt `VehicleConstraint` |
| streaming | what a tile costs to fetch, bake and place at each rung; the cache budget that holds Shibuya under 512 MB (board:2104) | Cesium's SSE scheduler |
| audio | engine notes from the machine, ambience by place and hour, occlusion | Steam Audio, Resonance |
| save and replay | scenario + snapshot + event log as one schema (board:2151); a save restored renders the next frame bit-identical | Unreal `DemoNetDriver` checkpoints |

