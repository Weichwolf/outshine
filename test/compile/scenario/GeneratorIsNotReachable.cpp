/* A subject is a generator INVOCATION -- a name and the parameters that generator declares. Naming
 * the generator itself here would put a content taxonomy inside the declaration. */
#include "Generator.h"
int main() { return 0; }
