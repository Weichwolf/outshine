/* A PROVIDER MAY NOT NAME THE ENGINE. A source that needed the world to answer *what do you cover*
 * would have the arrow the wrong way round, so the streamer has no spelling in this include set and
 * this file must not compile. */
#include "TilePool.h"
int main() { return 0; }
