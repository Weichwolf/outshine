/* A PROVIDER MAY NOT NAME THE ENGINE. A source that needed the world to answer *what do you cover*
 * would have the arrow the wrong way round, and the include set is what makes the streamer, the
 * renderer, a generator and the log unspellable rather than discouraged. */

#include "Check.h"
#include "Layering.h"

int main() {
  using namespace outshine::Test;
  EveryCompileSubjectHolds("test/outshine/unit/compile/data");
  NoIncludeClimbsOutOfItsDirectory("src/data");
  Covers("I.22 each upstream is a provider that declares what it covers and knows nothing that consumes it");
  return Report();
}
