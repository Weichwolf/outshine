/* A DECLARATION SAYS WHAT IS TO BE SHOWN AND KNOWS NOTHING THAT SHOWS IT. The renderer is above the
 * declaration and below none of it, so this file must not compile. */
#include "Mod.h"
#include "Renderer.h"
// REFUSED: 'Renderer.h' file not found
int main() { return 0; }
