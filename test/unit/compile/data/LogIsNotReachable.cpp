/* A provider's I/O is the transport the host supplies and its own declared files, never the engine's
 * log: a refusal it cannot spell is a refusal its caller has to name, which is where the path is
 * known. */
#include "SourceSet.h"
#include "Log.h"
// REFUSED: 'Log.h' file not found
int main() { return 0; }
