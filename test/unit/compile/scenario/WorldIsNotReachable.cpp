/* A scene declares a standpoint; it may not name what streams to one. The band it refuses outside of
 * is core's (core/Mercator.h), which is why the refusal costs this include set nothing. */
#include "Mod.h"
#include "TilePool.h"
// REFUSED: 'TilePool.h' file not found
int main() { return 0; }
